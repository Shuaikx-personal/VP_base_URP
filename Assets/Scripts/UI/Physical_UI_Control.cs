using UnityEngine;

public class Physical_UI_Control : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }

    public void DoQuit()
    {
        Debug.Log("Quit");
        Application.Quit();
    }
}
